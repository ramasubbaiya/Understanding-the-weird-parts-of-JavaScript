/* Execution Context (Global)*/

// Global Object - Inside browsers
console.log(this);

//Window Object - Current Tab
console.log(window);

console.info('Each tab each window is its own execution Context');

/* Global Object (window) = 'this' */

/* GLOBAL */
console.info('GLOBAL - Not outside the function');
var libraryName = "lib 2";

/**
 * Check the library is already in global window
 * 
 * It is check the global name space/ global object - 
 * so it doesn't collide or override it
 * 
 * window.libraryName = window.libraryName || "lib 2"; 
 * 
 */